CREATE TABLE `GuviZenClassStudent` (
  `Name` Akash Kumar,
  `Email_id` akashsai1999,
  `course` Full_Stack_Development,
  `Location` Chennai
);

CREATE TABLE `Batch` (
  `Batch` B30WDTamil,
  `WeekDayStudent` Yes,
  `WeekEndStudent` NO,
  `Language_preference` Tamil
);

CREATE TABLE `Task` (
  `JAVA_SCRIPT` completed,
  `HTML` completed,
  `CSS` completed,
  `React_JS` completed,
  `MYSQL` completed,
  `MONGODB` completed,
  `NODE_JS` completed
);

CREATE TABLE `Mentor` (
  `JAVA_SCRIPT` SaiMohan,
  `HTML` SaiMohan,
   `CSS` SaiMohan,
  `React_JS` RaghavKumar,
  `MYSQL` RaghavKumar,
  `MONGODB` RaghavKumar,
  `NODE_JS` RaghavKumar
);